# Build artifacts
release/
*.zip

# Editor tooling: auto-generated by the Ketho WoW API extension per-workspace,
# and points at an absolute, machine-specific extension install path, so it
# shouldn't be committed. Re-opening the folder with the extension regenerates it.
.vscode/
.luarc.json